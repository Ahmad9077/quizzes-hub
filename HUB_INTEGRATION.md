# القرآن الكريم — الربط المعتمد بالهَب

وافق أحمد صراحة على النشر داخل الهَب في 15 سبتمبر 2026. المصدر المعتمد واجهة تجربة سورة البلد، commit `5445aaa`. بُني الربط في فرع `release/quran-kareem` فوق إصدار الإنتاج `e2053f0` الذي يضم 15 قصة؛ توسعة القصص غير المنشورة لم تدخل هذا الإصدار.

## الربط الفعلي
- مجلد `quran/` صفحة ثابتة على GitHub Pages. مسارات نسبية للبيانات والخطوط والصور، واختيار السورة عبر `#surah-90`.
- `script.js`: مجموعة مستقلة `quranCollection` ضمن `assignmentCatalog`، مثل `storyCollection`. لا تدخل اختبارات الدرجات أو التحديات أو التكيف.
- `index.html`: قالب `quranTileTemplate`، ظاهر للحسابات التي لديها تعيين `quran-al-balad` فقط. `styles.css` يضيف قواعد محددة لبطاقة القرآن، مع تحديث أرقام كاش الواجهة.
- `supabase/migrations/013_seed_quran_al_balad.sql` وseed في `schema.sql`: تسجيل واحد في `quizzes`؛ لا تعديل للمصادقة أو RLS أو العلاقات أو درجات الأطفال.
- صفحة القرآن تعيد استخدام `config.js` و`access-guard.js` وجلسة Supabase الحالية. لا تسجيل دخول ثانٍ ولا لوحة ولي أمر مستقلة. يمكن للمشرف تفعيل «القرآن الكريم» من صلاحيات المستخدم الموجودة في الهَب.
- لا تعيينات تلقائية للأطفال. لا ترحيل لبيانات التجربة الخاصة.

## ما يعمل
سبع سور: البلد، الفجر، الغاشية، الأعلى، الطارق، البروج، والانشقاق؛ مجموعها 159 آية بالرسم العثماني الأصلي من تنزيل 1.1، تسجيلات العفاسي الأصلية، الآية المفردة أو البسملة ثم البداية إلى الآية المختارة، اختبار ذاتي بإخفاء النص، أسهم تنقل وزخرفة أرقام الآيات، موضع قراءة محلي معزول لكل حساب وسورة.

لا تسجيل للميكروفون، لا ASR، لا درجات حفظ، لا تكرار آلي ولا سجل تدريب أو لوحة إضافية. هذه الميزات ألغيت صراحة من النطاق.

## الصوت والتشغيل
يشغّل المتصفح تسجيلات العفاسي من `verses.quran.com`. فهرس القارئ 7 يُحدّث من واجهة Quran Foundation العامة إذا انتهت صلاحيته (7 أيام)، بمهلة 12 ثانية والتحقق من عدد الآيات وترتيبها ومسار القارئ. لا تمديد للصلاحية دون نجاح جلب جديد. عند فشل المصدر تبقى القراءة متاحة مع رسالة واضحة؛ لا حاجة للماك أو وكيل برمجي أو خدمة مدفوعة إضافية.

النص: https://tanzil.net/docs/Text_License
الصوت وشروطه المستقلة: https://api-docs.quran.com/legal/developer-terms/
واجهة المصدر: https://api.quran.com/api/v4/recitations/7/by_chapter/90?per_page=50
الخطوط مع إشعارات SIL OFL داخل `quran/fonts/`. لا نعيد استضافة ملفات MP3.

## البيانات والخصوصية
مفتاح موضع القراءة `quizzes-hub:quran:<auth-user-id>:verse:<chapter-number>` يضم رقم الآية وطريقة العرض فقط، ولا يُرسل إلى قاعدة البيانات. لا يُعد هذا حفظًا مؤكدًا ولا يتزامن بين الأجهزة. تسجيل الخروج أو تبديل الحساب يوقف الصوت ويعيد المستخدم إلى الهَب. محو بيانات الموقع يحذف الموضع المحلي.

المحتوى القرآني عام على استضافة ثابتة؛ التحقق من الوصول ينظم استعمال الحساب وبطاقته، وليس تشفيرًا للنص العام. تبقى حماية ملفات الأطفال ضمن آليات الهَب الحالية، ولم تنشر قاعدة التجربة أو تسجيلات خاصة أو أسرار.

## نقل التجربة مستقبلًا
لم ينفذ نقل تلقائي. موضع المتصفح القديم `rihlat-quran:verse:90` على نطاق التجربة منفصل عن نطاق الإنتاج والحسابات؛ لا يمكن اعتباره تقدم حمود دون اختيار حسابه. يمكن إضافة استئناف اختياري لاحقًا برقم الآية بعد موافقة المستخدم.

السجل القديم الخاص على الماك لا يقرأه الموقع العام. إن طُلب نقله لاحقًا: تصدير محلي آمن، اختيار ملف حمود صراحة، dry-run للإضافات والتعارضات، وقيد فريد `(pilot_id,event_id)` مع بصمة الحمولة. تجاهل النسخ المطابقة وإيقاف التعارض دون استبدال تقدم أحدث أو تحويل تدريب إلى حفظ مؤكد. احتفظ بمعرف دفعة الاستيراد واختبر تشغيله مرتين.

## التحقق والتراجع
`tests/quran-release.cjs` يختبر واجهة الهاتف/اللوحي/الكمبيوتر، النص، موضع الحساب، خروج الحساب، حارس الوصول ببيانات اصطناعية، ربط البطاقة واستبعادها من الدرجات والتحديات، وتحديث فهرس الصوت الحقيقي ورفض غير المطابق. `tests/quran-audio.cjs` يشغّل ملفات العفاسي فعليًا عبر WebKit ويتحقق من الإيقاف والتسلسل وإخفاء النص والفشل.

تشغيل الاختبارات: شغّل خادم HTTP محليًا على 8871، ثم `PLAYWRIGHT_MODULE=<installed-playwright-module> node tests/quran-release.cjs` و`node tests/quran-audio.cjs` بالمتغير نفسه. هذه محاكاة وليست شهادة اختبار iPhone فعلي.

التراجع بعملية revert لالتزام إصدار القرآن على main ثم التحقق من بناء Pages. اترك صف الفهرس ومواضع المتصفح محفوظة؛ لا تحذف بيانات أو تعيينات مستخدمين لاسترجاع الواجهة. إعادة النشر تعيد استعمال المواضع ذاتها.

## توسعة 22 سبتمبر 2026
أضاف أحمد السور الست إلى النطاق، وألغى سقف استهلاك المشروع السابق. لم يطلب تغيير النموذج أو تفويض وكلاء. السور في `quran/data/surahs.json` بترتيب البلد ثم السور الجديدة. `scripts/import-quran-surahs.py` يجلب نطاق السور السبع ويكتب بعد اجتياز التحقق للجميع؛ ملفات MP3 تبث من المصدر. بقاء المعرف التاريخي `quran-al-balad` مقصود للحفاظ على التكليفات والروابط الحالية؛ التوسعة لا تحتاج migration أو تعديل بيانات أطفال.

شاشة الاختيار تعرض السور السبع في عمودين على الهاتف. ترتيب تحميل الملفات لا يغير ترتيب البطاقات. نتائج التحقق الحالية في `docs/quran-seven-surahs-*.json`؛ التقارير التي تقتصر على سورة البلد أرشيفية. النص القرآني من تنزيل لم يتغير. اسم سورة 84 في عنوان الواجهة يوافق طلب المستخدم «الانشقاق»؛ اسمها في بيانات فهرس Quran Foundation «الإنشقاق»، وهذا اختلاف عنوان فقط.
